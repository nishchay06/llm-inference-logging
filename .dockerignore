# Keep the build context (and image) small, and never bake secrets in.
.venv/
.git/
.gitignore
**/__pycache__/
*.pyc
.pytest_cache/
.env
.handoff/
*.log
pgdata*/
# frontend build artifacts — the Docker `frontend` stage builds these fresh
frontend/node_modules/
frontend/dist/
